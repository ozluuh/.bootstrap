---
- name: "Ensure: 'Flathub' repository is added"
  community.general.flatpak_remote:
    name: flathub
    state: present
    method: system
    flatpakrepo_url: "https://dl.flathub.org/repo/flathub.flatpakrepo"
    enabled: true
